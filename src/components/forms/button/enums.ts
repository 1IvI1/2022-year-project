export enum ButtonTypes {
    PRIMARY,
    SECONDARY,
    DEFAULT
}

export enum RoutePaths {
  INTRO = '/intro',
  LOGIN = '/login',
  REGISTER = '/register',
  MAIN = '/main',
  ROOT = '/',
  MENU = '/menu',
  CART = '/cart',
  TRACKER = '/tracker'
}